version: '3'
services:
  php:
    image: php:7.4-fpm
    volumes:
      - ./src:/var/www/html

  nginx:
    image: nginx:latest
    ports:
      - 0.0.0.0:8080:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./src:/var/www/html
